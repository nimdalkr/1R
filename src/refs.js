/* 1R — optional built-in reference board.
   Public build intentionally ships without personal room photos.
   Users can add their own reference image locally from the Reference tab. */
window.RS.references=[];
